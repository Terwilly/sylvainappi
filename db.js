const db = require('./data/db.json');
const products = require('./data/products.json');
const signupUsers = require('./data/signupUsers.json');
const cart = require('./data/cart.json');

module.exports = {
  db,
  products,
  signupUsers,
  cart,
};
